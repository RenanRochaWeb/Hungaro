//page with two vertical selectors for the specified piece of clothing
//get what piece of clothing to make from its parent component as a default (homepage if navigated that way) or after pressing a button
//send that info to the selector after getting it from the backend

export default function Clothing() {
  return (
    <>
      <div>Clothing Page</div>
    </>
  );
}
